/* tabuada no console de 0 a 10 no formato:
multiplicador x número = resultado  */

const multiplicador = 7;

for(let i = 0; i <= 10; i ++){
    const calc = multiplicador * i;
    console.log(`${multiplicador} x ${i} = ${calc}`);
};